// Rental timing (minutes)
export const BUFFER_MINUTES = 5;
export const RENTAL_DURATION_MINUTES = 2 * 60; // 2 hours
export const GRACE_MINUTES = 5; // after rental end before marked overdue
export const WARN_BEFORE_END_MINUTES = 15; // show "approaching limit" (amber)
