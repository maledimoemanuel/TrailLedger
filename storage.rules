rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Authenticated users (staff) can read/write bike photos
    match /bikes/{bikeId}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
